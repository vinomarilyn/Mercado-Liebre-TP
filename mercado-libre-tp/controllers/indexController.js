const controller = {
  index: (req, res )=> {
    res.render('home');//render nos permite ver la pagina
      },

  //login: (req, res )=> {
 // res.render('login');//render nos permite ver la pagina
   // }
 };

module.exports = controller ;